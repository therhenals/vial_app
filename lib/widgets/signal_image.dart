import 'package:flutter/material.dart';

class SignalImage extends StatelessWidget {
  const SignalImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
